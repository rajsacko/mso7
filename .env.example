# MSO7 / Maison Edit — copy to .env.local

# Required for speech-to-caption extraction from uploaded video
OPENAI_API_KEY=

# Safety cap for render jobs (seconds of usable clip time)
MAX_RENDER_SECONDS=180

# Optional: Chrome path when not using Docker's chromium
# REMOTION_CHROME_EXECUTABLE_PATH=
